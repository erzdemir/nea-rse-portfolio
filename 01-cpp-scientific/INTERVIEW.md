# C++ toolkit — five projects (spoken notes)

## 1 xs_lookup
Declared interpolation. No silent extrapolation.

## 2 xs_compare
Two evaluations, one tolerance. JANIS-shaped.

## 3 tape_audit
MAT/MF/MT/T required. Incomplete tape fails closed.

## 4 xs_condense
Pointwise table to a declared group structure. Not NJOY GROUPR.

## 5 process_pipeline
Production shape: tape -> load table -> thermal lookup -> JSON audit.

Live demo:
  ./build/process_pipeline --tape data/pipeline.tape

Sentence:
  Exploratory code is the interpolation experiment.
  Production code is this pipeline: loud failures and an audit record.
  The physics processor in production is NJOY or FRENDY;
  the contract around it looks like Projects 1-5.
